# lab2-2 Backend

Simple Node.js backend using Express.

## Available Scripts

- `npm start` - run the server with Node
- `npm run dev` - run the server with nodemon for development

Server listens on port 3000 by default (use `PORT` env variable to override).

```bash
npm install
npm run dev
```
